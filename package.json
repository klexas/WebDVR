{
  "name": "vid-dl",
  "version": "0.1.0",
  "description": "Video stream finder and downloader",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "electron .",
    "dev": "tsc && electron ."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "concurrently": "^8.0.0",
    "electron": "^41.1.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@cliqz/adblocker-electron": "^1.34.0",
    "cross-fetch": "^4.1.0"
  }
}
